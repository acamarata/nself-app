# Environment files — never commit these
.env.local
.env*.local
.env.dev
.env.production
.env.staging

# .env.example IS intentionally committed — it is the template for new devs

# Push notification credentials — real Firebase project secrets, never commit
google-services.json
GoogleService-Info.plist
