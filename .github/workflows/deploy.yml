# DEAD STUB — to be retargeted for web/ntask Vercel deploy in Epic G-S3-T11.
name: Deploy

# Disabled: Next.js frontend removed in commit a3ed9c4 (Flutter is now canonical).
# Flutter web deployment TBD — re-enable once flutter build web is wired up.
on:
  workflow_dispatch:

jobs:
  placeholder:
    name: Deploy (disabled)
    runs-on: ubuntu-latest
    steps:
      - run: echo "Flutter web deployment not yet configured."
