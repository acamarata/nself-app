name: Desktop CI

# PENDING: Epic E will activate this for desktop builds. Flutter desktop removed.
on:
  workflow_dispatch:

jobs:
  placeholder:
    name: Desktop CI (disabled)
    runs-on: ubuntu-latest
    steps:
      - run: echo "Flutter desktop builds handled by flutter-ci.yml."
