name: Multi-Arch Check

# Asserts multi-architecture build compatibility.
# Repo type: TypeScript app surfaces + nSelf backend (nself-org/ntask)
# Authority: S30-T05, STORM-CROSS-PLATFORM
# Template: .claude/docs/doctrines/multi-arch-check.workflow.yml

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  multi-arch-stub:
    name: Multi-arch check (N/A)
    runs-on: ubuntu-latest

    steps:
      - name: Report repo type
        run: |
          echo "Repo type: TypeScript app surfaces (RN/Expo, Vite, Tauri, rn-tvos) + nSelf backend"
          echo "No Go binaries or Docker images produced. Multi-arch check N/A. PASS."

