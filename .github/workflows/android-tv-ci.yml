name: Android TV CI

# PENDING: Epic F will activate this for React Native TV builds.
on:
  push:
    branches: [main]
    paths: ['.github/workflows/android-tv-ci.yml']
  pull_request:
    branches: [main]
    paths: ['.github/workflows/android-tv-ci.yml']
  workflow_dispatch:

jobs:
  android-tv-ci:
    name: Android TV CI (disabled)
    runs-on: ubuntu-latest
    steps:
      - run: echo "Android TV support not yet implemented in Flutter. Tracking issue for v1.2.0."
