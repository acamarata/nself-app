# Generated by nself build - DO NOT EDIT MANUALLY
# Project: ntask

user nginx;
worker_processes auto;
pid /var/run/nginx.pid;

events {
    worker_connections 2048;
    multi_accept on;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    server_names_hash_bucket_size 128;
    server_tokens off;
    client_max_body_size 100M;

    # SSL Grade A — Mozilla Modern configuration
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 1d;
    ssl_session_tickets off;
    ssl_stapling on;
    ssl_stapling_verify on;
    # Docker's embedded DNS (127.0.0.11) resolves container names on the
    # user-defined bridge network. valid=10s keeps the cache short so a
    # recreated container (new IP) is picked up on the next request instead
    # of causing 502s until a manual `nginx -s reload`. Only takes effect
    # for proxy_pass targets expressed via an nginx variable (see
    # service.conf.tmpl LazyResolve) — static `upstream {}` blocks resolve
    # once at load time regardless of this directive.
    resolver 127.0.0.11 valid=10s ipv6=off;
    resolver_timeout 5s;

    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/json
        application/javascript
        application/xml
        application/xml+rss
        application/vnd.api+json
        image/svg+xml
        font/woff
        font/woff2;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent';
    access_log /var/log/nginx/access.log main;
    error_log /var/log/nginx/error.log warn;

    # Empty (not "close") for non-websocket requests so upstream keepalive
    # connections are reused instead of torn down after every request.
    map $http_upgrade $connection_upgrade {
        default upgrade;
        ''      "";
    }

    include includes/rate-limits.conf;
    include /etc/nginx/conf.d/*.conf;
    include /etc/nginx/conf.d-staging/*.conf;
    include /etc/nginx/sites/*.conf;
}
