# =============================================================================
# ROOT-LEVEL DOCS DIRECTORY (clean-root violation — content migrated to .github/wiki/)
# =============================================================================
# docs/RELEASE.md was migrated to .github/wiki/Deployment.md merge in Epic I (2026-06-27).
# Remove from tracking: git rm -r --cached docs/ && git commit -m "chore: remove docs/ clean-root violation"
# NOTE: Anchored to root (/docs/) so .github/docs/ is NOT excluded (it's tracked).
/docs/

# =============================================================================
# FLUTTER / DART BUILD ARTIFACTS (legacy — app/ removed in RN migration)
# =============================================================================
# app/ directory: leftover Flutter build artifacts from pre-RN-migration Flutter
# client. The Flutter app/ was deleted in feat(mobile): migrate Flutter to React
# Native Expo (7c594b5). These disk remnants are not tracked; suppress via gitignore.
# Anchored to root so apps/mobile/src/app/ (React Native screens) is NOT excluded.
/app/
**/.dart_tool/
**/build/
**/*.g.dart
**/*.freezed.dart

# =============================================================================
# AI AGENT DIRECTORIES (private — never commit)
# =============================================================================
.ai/

# =============================================================================
# ROOT-LEVEL PRIVATE FILES
# =============================================================================
READY.md
PROMPT.md

# =============================================================================
# PLANNING & WORKING DOCUMENTS (local only)
# =============================================================================
TODO.md
DONE.md
FEEDBACK.md
NEXT.md
TRACKING.md
PLAN.md

# =============================================================================
# OS FILES
# =============================================================================
.DS_Store
Thumbs.db

# =============================================================================
# IDE & EDITORS
# =============================================================================
.vscode/
!.vscode/settings.json
.idea/
*.swp
*.swo
*~

# =============================================================================
# ENVIRONMENT & CREDENTIALS (never commit)
# =============================================================================
.env
.env.*
!.env.example
!.env.*.example
.env.local
.env.*.local
.secrets/
secrets/
*.key
*.pem

# =============================================================================
# PROJECT-SPECIFIC PRIVATE DIRECTORIES
# =============================================================================
.backend/
.nself/
backend/.runtime/
backend/runtime/

# =============================================================================
# DEPENDENCIES
# =============================================================================
node_modules/
/.pnp
.pnp.js

# =============================================================================
# BUILD OUTPUT
# =============================================================================
dist/
build/
.next/
out/
.releases/*
!.releases/README.md
!.releases/android/
!.releases/android/README.md

# =============================================================================
# LOGS
# =============================================================================
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs/

# =============================================================================
# CACHE & TEMP
# =============================================================================
.cache/
.tmp/
tmp/
.wiki-tmp/
*.tmp
*.temp

# =============================================================================
# TEST OUTPUT
# =============================================================================
coverage/
test-results/
playwright-report/
playwright/.cache/

# =============================================================================
# TYPESCRIPT
# =============================================================================
*.tsbuildinfo
next-env.d.ts

# =============================================================================
# DEPLOYMENT PLATFORMS
# =============================================================================
.vercel

# =============================================================================
# LANGUAGE-SPECIFIC
# =============================================================================

# Python
__pycache__/
*.pyc

# Rust / Go
target/
bin/

# Terraform
*.tfstate
*.tfstate.*
.terraform/
.claude/
.codex/

# Environment files (sensitive)
.env.secrets
_backup*
.volumes/
.vscode/*
.cursor/
.aider/
.aider.chat.history.md
.continue/
.windsurf/
.gemini/
.codeium/
local.properties
.vscode/*
.idea/
.claude/
.codex/
.cursor/
.aider/
.aider.chat.history.md
.continue/
.windsurf/
.gemini/
.codeium/
.vscode/*
.idea/
.claude/
.codex/
.cursor/
.aider/
.aider.chat.history.md
.continue/
.windsurf/
.gemini/
.codeium/
.claude/
.vscode/*
.idea/
.claude/
.codex/
.cursor/
.aider/
.aider.chat.history.md
.continue/
.windsurf/
.gemini/
.codeium/
.claude/

# =============================================================================
# DOCKER LOCAL OVERRIDES (never commit — machine-specific)
# =============================================================================
docker-compose.dev.yml
docker-compose.override.yml
backend/docker-compose.yml

# Exception: backend/docker-compose.override.yml IS committed. Unlike a
# personal/machine-specific override, it carries durable fixes for real
# `nself build` generator gaps (functions service read_only/port/healthcheck
# mismatch, missing ACTION_HANDLER_URL passthrough to hasura) that every dev
# and CI runner needs — see the file's own header comments + CLI gaps filed:
# nself-functions-readonly-fs, nself-functions-healthcheck-port-mismatch,
# nself-hasura-action-env-passthrough. Contains no secrets (verified — only
# read_only/port/healthcheck/env-var-name config). Remove this exception once
# those CLI gaps are fixed upstream and the override is no longer needed.
!backend/docker-compose.override.yml
# Generated by nself build (contains admin secret) — use config.yaml.example
backend/hasura/config.yaml

# Expo prebuild output (generated by `npx expo prebuild`; contains debug keystore)
apps/mobile/android/
apps/mobile/ios/
apps/mobile/.expo/

# Shared @nself packages are cloned in at install time, never committed.
/packages/
